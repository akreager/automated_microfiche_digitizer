# Segmentation

Document grid detection and per-document tile manifest generation.

Uses Hough line detection on a fiche overview image to identify the document grid. Handles variable document sizes, polarity variation (positive/negative), and outputs per-document tile manifests for the stitching stage.
